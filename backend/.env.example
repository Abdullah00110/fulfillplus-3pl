# Copy this file to ".env" and fill in your real values.
# (On Windows: rename ".env.example" to ".env")

SECRET_KEY=dev-only-secret-change-me
DEBUG=True

# PostgreSQL settings
DB_NAME=fulfillplus
DB_USER=postgres
DB_PASSWORD=root
DB_HOST=localhost
DB_PORT=5432
